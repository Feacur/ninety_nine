#if !defined(ENGINE_PLATFORM_OPENGL_LIBRARY)
#define ENGINE_PLATFORM_OPENGL_LIBRARY

#include "engine/api/types.h"

void * engine_opengl_get_function(cstring name);

#endif // ENGINE_PLATFORM_OPENGL_LIBRARY
